/**
 * Domain Shared Types
 * 
 * Common types used across domain modules.
 * Import from here to avoid export conflicts.
 */

/**
 * UUID type alias for exercise/session/athlete IDs
 */
export type UUID = string;

/**
 * Common date string format (ISO 8601)
 */
export type ISODateString = string;
